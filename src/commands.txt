process -input="10"

regex -input=^ABC(01001)*09$

regex -input=9*

copilot helped generating the cli, and print

fro nfa's/dfa checking, check for state and match for input, if more than one same input, the it is a nfa